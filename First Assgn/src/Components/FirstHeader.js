import {Image} from "./Image";

const FirstHeader = () => {
    return(
        <header className="App-Header">
            <Image width = {100}/>
            <h1>React</h1>
        </header>
    )
}

export default FirstHeader;